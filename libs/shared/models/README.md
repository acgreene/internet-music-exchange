# @ime/models

Isomorphic domain types shared by the client apps and the API. Tagged `scope:shared`.

Pure TypeScript only: no Angular, no Node, no DOM. Everything here must be safe to import from anywhere in the workspace.
