# client-artist-pages

The trusted host that renders an untrusted designer page in a sandboxed,
cross-origin iframe and speaks to it over the `designer-page:` postMessage
protocol. Imported via `@ime/client-artist-pages`.
