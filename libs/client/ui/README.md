# @ime/ui

Shared Angular UI components for the client apps (web and mobile). Tagged `scope:client`.

Components here must be presentational and platform-agnostic: no direct API calls, no Node or Capacitor imports.
